
  <div id="<%= dom_id post %>">
    <h4><%= post.title %></h4>
    <h4>posted by<%= link_to post.user.name, user_path(post.user) %></h4>
    
    <h6><%= pluralize(post.views, "view")  %></h6>

    <p>
      <%= post.body %>
    </p>

  </div>
